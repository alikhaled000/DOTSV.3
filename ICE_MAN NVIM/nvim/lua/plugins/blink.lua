-- blink.cmp: fast completion engine
-- Avante sources removed since avante.nvim is disabled.
-- LazyVim's default blink config is sufficient — this file only overrides if needed.

return {
  "saghen/blink.cmp",
  -- Let LazyVim manage blink defaults; only add overrides here
  opts = {
    -- Add any custom completion sources or tweaks here
    -- e.g. keymap overrides, appearance tweaks
  },
}
